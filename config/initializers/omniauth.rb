# frozen_string_literal: true

# Google OAuth Configuration
# This file is intentionally minimal as we're using Signet for manual OAuth flow
# rather than OmniAuth middleware to avoid session requirements in API-only mode
